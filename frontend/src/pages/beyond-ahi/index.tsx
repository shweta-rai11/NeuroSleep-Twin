import { PageStub } from "@/components/layout/PageStub";

export default function BeyondAhiPage() {
  return (
    <PageStub
      title="Beyond AHI"
      description="Compares AHI/ODI against oxygen, arousal, autonomic, and recovery burden side by side — explicitly framed as exploring physiology alongside AHI, never as a replacement for it."
      phase={11}
      specSection="§18 Beyond AHI"
    />
  );
}
